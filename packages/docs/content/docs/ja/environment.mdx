---
title: 環境変数
description: 環境変数で OMPChamber と OMP 連携を設定します。
---

# 環境変数

OMPChamber は起動時にこれらの環境変数を読み取ります。スタートアップサービスでは、`ompchamber startup enable` がデフォルトで現在の環境をスナップショットするため、サービスに使わせたい変数を変更した後は再実行してください。

## OMPChamber サーバー

### `OMPCHAMBER_HOST`

OMPChamber Web サーバーのバインドアドレスです。他のマシンからアクセスできるようにするには `0.0.0.0` を使います。

### `OMPCHAMBER_UI_PASSWORD`

ブラウザ UI のパスワードです。localhost 以外にバインドする場合、トンネルを使う場合、またはリバースプロキシの背後で実行する場合に使います。

### `OMPCHAMBER_API_ONLY`

`true` または `1` に設定すると、OMPChamber をヘッドレスモードで起動します。デスクトップおよびモバイルクライアント向けの API ルートは利用できますが、ブラウザ UI は配信されません。

### `OMPCHAMBER_DATA_DIR`

OMPChamber のデータディレクトリを上書きします。デフォルトは `~/.config/ompchamber` です。

### `OMPCHAMBER_COMPRESS_API`

API レスポンス圧縮を制御します。`true` または `1` で強制的に有効化し、`false` または `0` で強制的に無効化します。

### `OMPCHAMBER_SKIP_API_COMPRESSION`

`true` または `1` に設定すると API レスポンス圧縮を無効化します。これは `OMPCHAMBER_COMPRESS_API` より優先されます。

### `OMPCHAMBER_VERBOSE_REQUEST_LOGS`

`true` または `1` に設定すると詳細な HTTP リクエストログを有効にします。

### `OMPCHAMBER_UPDATE_API_URL`

更新確認 API エンドポイントを上書きします。ほとんどのユーザーは未設定のままでかまいません。

### `OMPCHAMBER_PACKAGE_MANAGER`

自動検出が間違っている場合に、更新操作で使うパッケージマネージャーを強制します。

## OMP サーバー

### `OPENCODE_HOST`

OMPChamber を既存の OMPChamber サーバーに接続します。値は明示的なポートを含み、パス、クエリ、ハッシュを含まない `http` または `https` の origin である必要があります。`OPENCODE_HOST` は `OPENCODE_PORT` より優先されます。

### `OPENCODE_PORT`

OMP サーバーのポートを設定します。管理対象 OMP では管理対象ポートの要求になり、`OPENCODE_SKIP_START=true` ではそのポートの外部サーバーに接続します。

### `OPENCODE_SKIP_START`

`true` に設定すると、OMPChamber が独自の OMP サーバーを起動しません。

### `OMPCHAMBER_OPENCODE_HOSTNAME`

OMPChamber が管理する OMP サーバーのバインドホスト名です。デフォルトは `127.0.0.1` です。

### `OPENCODE_BINARY`

OMPChamber が実行する `omp` 実行ファイルへのパスです。

### `OPENCODE_CONFIG`

特定の OMP 設定ファイルへのパスです。

### `OPENCODE_CONFIG_DIR`

エージェント、スキル、スニペット、設定検出に使う特定の OMP 設定ディレクトリへのパスです。

### `OPENCODE_DATA_DIR`

管理対象 OMP サーバーのカスタムデータディレクトリです。

### `OPENCODE_WSL_DISTRO`

Windows 上の OMP 連携に使う WSL ディストリビューションを選びます。

### `OMPCHAMBER_OPENCODE_WSL_DISTRO`

WSL ディストリビューション選択用の OMPChamber 固有エイリアスです。両方が設定されている場合は `OPENCODE_WSL_DISTRO` が優先されます。

### `OPENCODE_JWT_SECRET`

UI 認証トークンの署名に使うシークレットです。永続的なサービス配置では長いランダム値を使ってください。

## ターミナルと Git

### `OMPCHAMBER_TERMINAL_SHELL`

OMPChamber のターミナルセッションで使うシェル実行ファイルです。

### `OMPCHAMBER_GIT_BINARY`

OMPChamber の Git 機能で使う Git 実行ファイルです。

### `GIT_BINARY`

代替の Git 実行ファイル上書きです。OMPChamber 固有の設定には `OMPCHAMBER_GIT_BINARY` を推奨します。

### `OMPCHAMBER_GIT_READ_CACHE_TTL_MS`

Git に基づくファイル読み取りキャッシュの有効期間（ミリ秒）です。デバッグ中にこのキャッシュを無効化するには `0` を設定します。

## 音声とトンネル

### `OPENAI_API_KEY`

OpenAI 互換サービスを呼び出す OMPChamber の音声機能で使う API キーです。

### `OMPCHAMBER_ALLOW_REMOTE_OPENAI_COMPAT_URLS`

`true` または `1` に設定すると、音声機能でリモートの OpenAI 互換 base URL を許可します。

### `NGROK_AUTHTOKEN`

OMPChamber のトンネルコマンドで使う ngrok 認証トークンです。`ngrok config add-authtoken <token>` で ngrok 側に設定することもできます。

## ランタイムヘルパー

### `BUN_BINARY`

デーモンプロセスを起動するときに OMPChamber が使う Bun 実行ファイルです。

### `BUN_INSTALL`

Bun のインストールルートです。OMPChamber はデーモン起動と更新のために `bin/bun` を探す際に使います。

### `VITE_OPENCODE_URL`

Vite でビルドされた Web アプリ向けのビルド時 API base URL です。通常の CLI またはデスクトップ利用では、ほとんどのユーザーは設定する必要がありません。
