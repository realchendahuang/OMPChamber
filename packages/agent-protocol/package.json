{
  "name": "@ompchamber/agent-protocol",
  "version": "2.0.0",
  "private": true,
  "description": "OMPChamber agent domain protocol types and adapters. UI and server both depend on this; neither depends on harness types.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "build": "tsc --noEmit",
    "lint": "node -e \"process.exit(0)\""
  },
  "exports": {
    ".": "./src/index.ts",
    "./types": "./src/types.ts",
    "./domain-types": "./src/domain-types.ts"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
